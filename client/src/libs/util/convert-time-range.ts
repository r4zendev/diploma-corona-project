import { Moment } from 'moment';

export function convertTimeRange(_date1: Moment, _date2: Moment) {
  // Return two readable dates, second argument in return array is a date to send to api in order to receive
  // all stats from certain date.
}
